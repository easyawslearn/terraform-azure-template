name: 'Terraform GitHub Actions'
on:
  pull_request:
    branches:
      - master

jobs:
  fmt:
    name: 'terraform fmt'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v2.3.4

      - name: 'Terraform Format'
        uses: 'easyawslearn/github-actions@v6.0'
        with:
          actions_subcommand: 'fmt'

  validate:
    name: 'terraform validate'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v2.3.4

      - name: 'Terraform init'
        uses: 'easyawslearn/github-actions@v6.0'
        with:
          actions_subcommand: 'init'

      - name: 'Terraform validate'
        uses: 'easyawslearn/github-actions@v6.0'
        with:
          actions_subcommand: 'validate'
  plan:
    name: 'terraform plan'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v2.3.4

      - name: 'Terraform init'
        uses: 'easyawslearn/github-actions@v6.0'
        with:
          actions_subcommand: 'init'

      - name: 'Terraform plan'
        uses: 'easyawslearn/github-actions@v6.0'
        with:
          actions_subcommand: 'plan'
          
